# Tip Splitter

A tiny single-page web tool that splits a bill (plus tip) between people.

Built as a hands-on project for learning the GitHub workflow: repositories,
commits, branches, pull requests, branch protection, Actions, and continuous
deployment.

## Run it locally

Open `index.html` in any web browser. That's it — no build step, no dependencies.
